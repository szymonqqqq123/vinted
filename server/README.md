# Social Media Manager

This project contains a minimal proof-of-concept for a Buffer/Hootsuite-like application.

## Client
- Next.js with Tailwind CSS
- `npm run dev` in `client` to start

## Server
- Node.js with Express and MongoDB
- `npm run dev` in `server` to start
